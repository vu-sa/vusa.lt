/**
 * File/document components driven by a polymorphic `fileable` ({ id, type }),
 * so they work for any model the SharePoint integration knows about.
 */

export { default as FileablePreviewCard } from './FileablePreviewCard.vue';

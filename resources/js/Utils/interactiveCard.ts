/**
 * Shared styling recipe for interactive (clickable/navigable) admin cards, tiles and list rows.
 *
 * Cards are flat (no shadow, square corners) at rest; on hover they gain a subtle
 * brand border tint and a lifted secondary background.
 */
export const interactiveCardClass
  = 'transition-colors hover:border-brand/40 hover:bg-secondary/40';
